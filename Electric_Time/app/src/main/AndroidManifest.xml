<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.electric_time">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ExtraMenuActivity"></activity>
        <activity android:name=".HovertraxHoverboardActivity" />
        <activity android:name=".SegwayNinebotSPlusActivity" />
        <activity android:name=".EvolveBambooGTR2in1Activity" />
        <activity android:name=".BoostedMiniSActivity" />
        <activity android:name=".WalkingActivity" />
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>